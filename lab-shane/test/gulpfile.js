const expect = require('chai').expect;
const index = require('../lib/index');

describe('the readFiles function', function(){
  it('should do stuff', function(){
    expect(index()).to.eql('I love you');
  });
});
