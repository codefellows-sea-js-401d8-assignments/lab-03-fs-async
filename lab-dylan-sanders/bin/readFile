#!/usr/bin/env node
var readFiles = require('../lib/readFiles');

var readFilescli = exports = module.exports = function(array, writeStream) {
  writeStream = writeStream || process.stdout;
  array = array || process.argv[2];
  array = array || [];
  writeStream.write(readFiles(array));
};

readFilescli();
