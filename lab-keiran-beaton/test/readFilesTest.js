const expect = require('chai').expect;
const readFiles = require('../lib/readFiles');
var readFilescli = require('../bin/readFilesCli');

describe('readFilesCli', function() {
  it('should console.log the files in the array', function() {
    expect();
  });
});
