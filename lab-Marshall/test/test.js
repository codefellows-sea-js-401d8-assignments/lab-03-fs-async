const expect = require('chai').expect;
const makeReadFiles = require('../lib/readFiles');

describe('index async test', function(){
  it('should log in order', function(){
    expect(true).to.eql(true);
  });
});
